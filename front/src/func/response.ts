export const signupSuccess = '계정이 성공적으로 생성되었습니다';
export const isSingupSuccess = (message: string) => {
  return message === signupSuccess;
};

export const loginSuccess = '성공적으로 로그인 했습니다';
export const isLoginSuccess = (message: string) => {
  return message === loginSuccess;
};
